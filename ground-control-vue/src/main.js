import { createApp } from 'vue'
import './assets/style.css'
import App from './App.vue'

createApp(App).mount('#app')
// This is the main entry point for the Vue application.
// It imports the necessary modules, styles, and the main App component.
// The createApp function is used to create a Vue application instance,
// which is then mounted to the HTML element with the id 'app'.
// The application uses a global CSS file for styling.
// The main.js file is essential for bootstrapping the Vue application.
// It serves as the starting point for the Vue application, linking the App component to the DOM.